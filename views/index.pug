extends layout

block content
  .container
    .jumbotron
      form(action='/login', method='get')
          .form-group.row
            .col-xs-1
                button.btn.btn-default(type='submit') login

