extends layout

block content
	.container
		.jumbotron
			//form(action='/action', method='post')
					.form-group.row
						.col-xs-2
							button.btn.btn-default(type='submit' name="action" value="logout") logout
						.col-xs-2
							button.btn.btn-default(type='submit' name="action" value="pub") publish